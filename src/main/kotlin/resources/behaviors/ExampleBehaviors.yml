# ============================================
#          自定义行为脚本 (Kether)
# ============================================

# 示例：吸血脚本
vampire_logic: |-
  # 计算伤害的 10% 作为回复量
  set heal_amount to ( &damage_data.getFinalDamage * ( &value / 100 ) )
  # 给攻击者回血
  ac heal &attacker &heal_amount
  # 播放粒子效果 (如果装了其他扩展)
  # effect lib ...